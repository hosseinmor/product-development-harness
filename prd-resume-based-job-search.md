---
id: prd-resume-based-job-search
artifact: prd
owner: unresolved
references:
  - "http://platform-eng.pages.git.jvoffice.ir/documents/jobvision/contents/job-search/0_overview/"
  - "http://platform-eng.pages.git.jvoffice.ir/documents/jobvision/contents/cv/0_overview/"
  - "http://platform-eng.pages.git.jvoffice.ir/documents/jobvision/contents/cv/entity-cv/"
  - "http://platform-eng.pages.git.jvoffice.ir/documents/jobvision/contents/jobpost/entity-jobpost/"
  - "http://platform-eng.pages.git.jvoffice.ir/documents/jobvision/contents/00-open-questions/"
  - "https://jobvision.ir/jobs"
  - "https://jobvision.ir/my-cv"
---

# جست‌وجوی شغل بر اساس رزومه

## Problem

کارجو برای رسیدن به آگهی‌های متناسب باید نیت شغلی خود را به عبارت جست‌وجو، دستهٔ شغلی و فیلترهای صفحهٔ نتایج ترجمه کند و آن‌ها را دستی تنظیم کند؛ در حالی که بخشی از اطلاعات لازم برای این کار قبلاً به‌صورت ساختاریافته در رزومه و رکورد کارجو وجود دارد. این تکرار، شروع جست‌وجوی متناسب را پرزحمت می‌کند و از کارجو می‌خواهد منطق فیلترهای محصول را خودش به اطلاعات رزومه‌اش نگاشت کند.

## Affected Users

- کارجوی ثبت‌نام‌شده

## Current Behavior

- صفحهٔ نتایج شغل از جست‌وجوی دستی بر اساس «عنوان شغلی یا سازمان»، دستهٔ شغلی و شهر پشتیبانی می‌کند.
- کنترل دستهٔ شغلی در صفحهٔ نتایج فعلی، در هر جست‌وجو یک دسته را می‌پذیرد.
- کارجو می‌تواند نتایج را با زمان انتشار، دورکاری، نوع همکاری، کارآموزی، حقوق، سابقهٔ کاری، سطح ارشدیت، مزایا و تسهیلات، صنعت، امکان استخدام افراد دارای معلولیت و امریهٔ سربازی محدود کند و ترتیب نتایج را تغییر دهد.
- فیلتر حقوق فعلی یکی از بازه‌های استاندارد حقوق را به‌صورت تک‌انتخابی می‌پذیرد. بازه‌های حقوق درخواستی فرم رزومه با بازه‌های فیلتر صفحهٔ نتایج یکسان نیستند؛ بعضی بازه‌ها هم‌پوشان یا میان دو بازهٔ جست‌وجو تقسیم می‌شوند. آگهی نیز حداقل و حداکثر حقوق ساختاریافته و یک تنظیم مستقل برای نمایش یا عدم نمایش مبلغ دارد؛ بنابراین «توافقی» بودن نمایش الزاماً به معنی نبود دادهٔ عددی آگهی نیست.
- صفحهٔ نتایج بنری دارد که کارجو را به صفحهٔ جداگانهٔ «فرصت‌های شغلی پیشنهادی» هدایت می‌کند؛ آگهی‌های پیشنهادی در خود مجموعهٔ نتایج جست‌وجو نمایش داده نمی‌شوند.
- رکورد کارجو محل سکونت و دسته‌های شغلی مورد علاقه را نگه می‌دارد؛ رزومهٔ ساختاریافته نیز حقوق درخواستی، عنوان شغلی شاخص و بخش‌هایی مانند سوابق شغلی، تحصیلات، زبان، نرم‌افزار و مهارت‌ها را دارد.
- هر سابقهٔ شغلی می‌تواند عنوان شغل، دستهٔ شغلی، صنعت و سطح ارشدیت داشته باشد. سابقهٔ کار به تفکیک دستهٔ شغلی نیز به‌صورت محاسبه‌شده نگهداری می‌شود و مشخص می‌کند کدام دسته مورد علاقهٔ کارجو است.
- آگهی‌ها برای دستهٔ شغلی، شهر، صنعت، سطح ارشدیت، نوع همکاری، سابقه، حقوق و چند شرط دیگر دادهٔ ساختاریافته دارند. همچنین برای سنجش تناسب رزومه و آگهی، وزن‌هایی برای یازده بُعد از جمله دستهٔ شغلی، صنعت، حقوق، سابقه، زبان، نرم‌افزار، سطح ارشدیت و تحصیلات وجود دارد.
- آگهی یک پرچم ساختاریافته برای پشتیبانی از استخدام افراد دارای معلولیت دارد. اگرچه این فیلد کارجو در Product Knowledge تأییدشده مستند نشده، بررسی current-product در فرم اطلاعات اولیهٔ `jobvision.ir/my-cv` وجود checkbox صریح «دارای معلولیت هستم» را تأیید کرد.

## Outcomes

### User Outcome

کارجو بتواند بدون تبدیل دستی اطلاعات رزومه‌اش به عبارت جست‌وجو و فیلترها، مستقیماً به مجموعه‌ای از آگهی‌های متناسب با همان اطلاعات برسد.

### Business Outcome

Business Outcome این تغییر هنوز توسط Product مشخص نشده است.

## Scope

- کارجو می‌تواند یک جست‌وجوی شغل مبتنی بر اطلاعات رزومه‌اش آغاز کند.
- سیستم زیرمجموعهٔ مناسب پارامترهای پشتیبانی‌شدهٔ جست‌وجو—مانند keyword، دستهٔ شغلی و فیلترهای مرتبط—را از اطلاعات رزومه و رکورد همان کارجو تعیین می‌کند.
- کارجو برای آغاز این جست‌وجو ملزم به وارد کردن عبارت یا تنظیم فیلتر نیست و مستقیماً به صفحهٔ نتایج همان جست‌وجو می‌رسد.
- این تغییر به ساخت یا تغییر خودکار رزومه و ارسال خودکار درخواست شغلی گسترش پیدا نمی‌کند.

## Key Product Scenarios

### جست‌وجو با اطلاعات کافی

1. کارجوی واجد شرایط، جست‌وجوی مبتنی بر رزومه را آغاز می‌کند.
2. سیستم از اطلاعات فعلی رزومه و رکورد همان کارجو، یک جهت شغلی اصلی و پارامترهای معتبر جست‌وجوی مرتبط با آن را تعیین می‌کند.
3. سیستم بدون درخواست ورود دستی عبارت یا فیلتر، جست‌وجو را اجرا می‌کند.
4. کارجو در صفحهٔ نتایجی قرار می‌گیرد که وضعیت جست‌وجوی آن معادل پارامترهای تعیین‌شده از رزومه است.

### اطلاعات ناکافی برای ساخت جست‌وجو

1. کارجو جست‌وجوی مبتنی بر رزومه را آغاز می‌کند.
2. سیستم هیچ دستهٔ شغلی مورد علاقه و هیچ عنوان یا دستهٔ شغلی ساختاریافته‌ای در سابقهٔ شاخص یا اخیر پیدا نمی‌کند.
3. سیستم جست‌وجوی مبتنی بر رزومه را اجرا نمی‌کند؛ چگونگی توضیح این وضعیت و هدایت کارجو برای افزودن اطلاعات، تصمیم Design است.

### نبود نتیجه در شهر محل سکونت

1. سیستم یک جهت شغلی معتبر و شهر محل سکونت را از اطلاعات کارجو تعیین می‌کند.
2. جست‌وجوی مقید به آن شهر نتیجه‌ای برنمی‌گرداند.
3. محصول نتیجهٔ خالی همان جست‌وجو را حفظ می‌کند و شهر را خودکار حذف یا گسترده‌تر نمی‌کند.

### حقوق درخواستی قابل استفاده

1. رزومهٔ کارجو یک بازهٔ استاندارد حقوق درخواستی دارد و حد پایین آن قابل استفاده است.
2. سیستم حد پایین بازه را کف حقوق کارجو در نظر می‌گیرد و آگهی‌هایی را که `MaxSalary` ذخیره‌شدهٔ آن‌ها کمتر از این کف است، به‌صورت سخت حذف می‌کند.
3. حد بالای بازهٔ حقوق درخواستی سقف محسوب نمی‌شود و نباید موجب حذف آگهی‌هایی با حقوق بالاتر شود. نمایش یا عدم نمایش مبلغ در کارت آگهی نیز این قاعده را تغییر نمی‌دهد.
4. اگر حقوق درخواستی معتبر وجود نداشته باشد، سیستم کف حقوق حدسی تولید نمی‌کند.
5. اگر ترکیب جست‌وجو نتیجه‌ای نداشته باشد، سیستم محدودیت حقوق را خودکار حذف نمی‌کند؛ کارجو همچنان می‌تواند آن را در صفحهٔ نتایج تغییر دهد.

### اعلام صریح معلولیت

1. کارجو در رزومه گزینهٔ «دارای معلولیت هستم» را انتخاب کرده است.
2. سیستم فیلتر سخت «امکان استخدام افراد دارای معلولیت» را به جست‌وجو اضافه می‌کند.
3. اگر ترکیب جست‌وجو نتیجه‌ای نداشته باشد، سیستم این فیلتر را خودکار حذف نمی‌کند.
4. اگر گزینهٔ رزومه انتخاب نشده باشد، سیستم این فیلتر را اعمال نمی‌کند و معلولیت را از هیچ دادهٔ دیگری استنباط نمی‌کند.

## Required Product Behavior

### منبع و ساخت جست‌وجو

- منبع شخصی‌سازی، اطلاعات فعلی رزومه و رکورد مرتبط با همان کارجو است.
- کارجو زمانی واجد شرایط است که دست‌کم یک دستهٔ شغلی مورد علاقه یا عنوان/دستهٔ شغلی ساختاریافته در سابقهٔ شاخص یا اخیر داشته باشد. درصد تکمیل رزومه معیار eligibility این قابلیت نیست.
- برای تعیین جهت شغلی، ترتیب تقدم عبارت است از: دسته‌های شغلی مورد علاقهٔ صریح؛ سپس عنوان یا دستهٔ سابقهٔ شغلی شاخص؛ سپس عنوان یا دستهٔ سابقهٔ اخیر.
- مهارت‌ها می‌توانند به تعیین keyword یا مرتبط‌تر کردن جهت به‌دست‌آمده کمک کنند، اما به‌تنهایی جهت شغلی جدیدی ایجاد نمی‌کنند.
- شهر محل سکونت، هرگاه در رکورد کارجو موجود و برای جست‌وجوی فعلی معتبر باشد، به‌عنوان فیلتر سخت اعمال می‌شود.
- حد پایین بازهٔ استاندارد حقوق درخواستی، هرگاه در رزومه موجود و معتبر باشد، کف حقوق کارجو است. هر آگهی که `MaxSalary` ذخیره‌شدهٔ آن کمتر از این کف باشد باید از نتایج حذف شود؛ حد بالای بازه سقف نیست و آگهی‌های با حقوق بالاتر را حذف نمی‌کند.
- قاعدهٔ سخت حقوق باید بر دادهٔ ساختاریافتهٔ ذخیره‌شدهٔ آگهی اعمال شود و مستقل از نمایش مبلغ یا نمایش «توافقی» باشد. نبودن حقوق درخواستی معتبر نباید به تولید یک کف حدسی منجر شود.
- اطلاعات گذشته یا واقعیت‌های رزومه—از جمله صنعت و سطح ارشدیت سوابق، مقدار سابقه، سن، جنسیت، تحصیلات، زبان و نرم‌افزار—نباید به‌تنهایی به فیلتر سخت و حذف‌کنندهٔ آگهی تبدیل شوند.
- زمان انتشار، دورکاری، نوع همکاری، کارآموزی، سابقهٔ مورد نیاز، سطح ارشدیت، مزایا، صنعت و امریهٔ سربازی نباید بدون یک ترجیح صریح و هم‌معنای کارجو به فیلتر سخت تبدیل شوند. دادهٔ فعلی رزومه برای این موارد یا ترجیح آینده را بیان نمی‌کند یا با معنای فیلتر جست‌وجو یکسان نیست.
- اگر گزینهٔ صریح «دارای معلولیت هستم» در رزومه انتخاب شده باشد، فیلتر «امکان استخدام افراد دارای معلولیت» باید به‌صورت سخت اعمال شود. اگر انتخاب نشده باشد، سیستم نباید این وضعیت را از اطلاعات دیگر استنباط کند یا فیلتر را اعمال کند.
- سیستم نباید برای شروع این مسیر، وارد کردن دستی keyword یا تنظیم دستی فیلتر را پیش‌نیاز کند.
- keyword، دستهٔ شغلی و فیلترهای موجود باید با معنای فعلی جست‌وجوی شغل سازگار بمانند. قاعدهٔ مصوب کف حقوق، معیار eligibility نتایج این قابلیت است و تفاوت بازه‌های فعلی UI نباید معنای آن را تغییر دهد.

### نتیجه و مرز قابلیت

- آغاز موفق این قابلیت باید به اجرای جست‌وجو و ورود مستقیم کارجو به صفحهٔ نتایج همان جست‌وجو منجر شود.
- مجموعهٔ نتایج باید خروجی موتور جست‌وجو پس از اعمال پارامترهای تولیدشده و قواعد سخت این PRD باشد؛ این PRD الگوریتم رتبه‌بندی جاری نتایج را بازتعریف نمی‌کند.
- اگر جست‌وجوی مقید به شهر محل سکونت نتیجه‌ای نداشته باشد، سیستم نباید شهر را خودکار حذف یا به محدودهٔ دیگری گسترش دهد.
- اگر محدودیت سخت حقوق اعمال شده و نتیجه‌ای وجود نداشته باشد، سیستم نباید آن را خودکار حذف یا به مقدار دیگری تبدیل کند.
- اگر فیلتر سخت «امکان استخدام افراد دارای معلولیت» اعمال شده و نتیجه‌ای وجود نداشته باشد، سیستم نباید آن را خودکار حذف کند.
- اگر کارجو هیچ‌یک از سیگنال‌های لازم برای تعیین جهت شغلی را نداشته باشد، سیستم نباید یک جست‌وجوی عمومی یا حدسی را به‌عنوان جست‌وجوی مبتنی بر رزومه اجرا کند.
- این قابلیت نباید رزومهٔ کارجو را تغییر دهد یا بدون اقدام جداگانهٔ کارجو، درخواست شغلی ایجاد کند.

### سیاست mapping

- هر بار آغاز این قابلیت باید دقیقاً یک جهت شغلی اصلی تولید کند.
- اگر چند دسته یا مسیر شغلی معتبر وجود داشته باشد، سیستم باید با رعایت ترتیب تقدم مصوب یک مورد را به‌عنوان جهت اصلی انتخاب کند؛ سایر جهت‌ها نباید در همان اقدام به جست‌وجوهای جداگانه یا یک جست‌وجوی چنددسته‌ای تبدیل شوند.

## Dependencies

- `job-search/0_overview.md`
- `cv/0_overview.md`
- `cv/entity-cv.md`
- `jobpost/entity-jobpost.md`

## Acceptance Criteria

- کارجوی واجد شرایط می‌تواند بدون وارد کردن keyword یا تنظیم فیلتر، جست‌وجوی مبتنی بر رزومه را آغاز کند و مستقیماً به صفحهٔ نتایج برسد.
- وضعیت جست‌وجوی مقصد شامل حداقل یک پارامتر جهت شغلی معتبر است که با ترتیب تقدم مصوب از اطلاعات فعلی همان کارجو به دست آمده باشد.
- هر بار آغاز این قابلیت دقیقاً یک جهت شغلی اصلی ایجاد می‌کند؛ وجود چند جهت معتبر نباید به جست‌وجوی چنددسته‌ای یا چند جست‌وجوی هم‌زمان منجر شود.
- اگر شهر محل سکونت کارجو موجود و برای جست‌وجو معتبر باشد، نتایج به همان شهر محدود می‌شوند؛ حتی اگر نتیجه‌ای وجود نداشته باشد این محدودیت خودکار حذف یا گسترده نمی‌شود.
- اگر بازهٔ استاندارد حقوق درخواستی معتبر باشد، حد پایین آن به‌عنوان کف اعمال می‌شود: آگهی با `MaxSalary` کمتر از کف در نتایج نیست؛ آگهی با `MaxSalary` برابر یا بیشتر باقی می‌ماند؛ حد بالای بازه آگهی‌های پردرآمدتر را حذف نمی‌کند؛ و نمایش «توافقی» این ارزیابی را تغییر نمی‌دهد. در نبود مقدار معتبر، هیچ کف حقوقی حدس زده نمی‌شود.
- زمان انتشار، دورکاری، نوع همکاری، کارآموزی، سابقهٔ مورد نیاز، سطح ارشدیت، مزایا، صنعت و امریهٔ سربازی صرفاً بر اساس واقعیت‌های فعلی رزومه به فیلتر سخت تبدیل نمی‌شوند.
- اگر کارجو در رزومه صریحاً گزینهٔ «دارای معلولیت هستم» را انتخاب کرده باشد، نتایج فقط به آگهی‌های دارای امکان استخدام افراد دارای معلولیت محدود می‌شوند و این محدودیت در وضعیت بدون نتیجه خودکار حذف نمی‌شود. در حالت انتخاب‌نشده، فیلتر اعمال نمی‌شود.
- کارجویی که دستهٔ شغلی مورد علاقه و عنوان/دستهٔ شغلی ساختاریافته در سابقهٔ شاخص یا اخیر ندارد، وارد یک جست‌وجوی عمومی یا حدسی با عنوان «جست‌وجو بر اساس رزومه» نمی‌شود.
- نتایج مقصد با معنای جاری پارامترهای اعمال‌شده در موتور جست‌وجوی شغل سازگارند.
- آغاز این قابلیت هیچ تغییری در رزومه ایجاد نمی‌کند و هیچ درخواست شغلی را خودکار ثبت نمی‌کند.

## Assumptions & Open Decisions

### Assumed

- برای ادامهٔ draft فرض شده است که مقصد، همان قابلیت استاندارد صفحهٔ نتایج است و رفتارهای فعلی آن پس از ورود—مانند تغییر جست‌وجو، فیلترها و ترتیب نتایج—حفظ می‌شوند. این فرض فقط برای جلوگیری از گسترش دامنه است و تصمیم Product محسوب نمی‌شود.
- دو سند تأییدشدهٔ حوزهٔ رزومه می‌گویند هر کارجو دقیقاً یک رزومه با نسخه‌های فارسی و انگلیسی دارد؛ فهرست سراسری پرسش‌های باز Product Knowledge هنوز همین موضوع را حل‌نشده نشان می‌دهد و منبع پیاده‌سازی برای رفع تعارض در دسترس نبود. برای draft، وجود یک رزومهٔ جاری فرض شده است. اگر منبع authoritative خلاف آن را نشان دهد، تعیین اینکه کدام رزومه مبنای جست‌وجوست به تصمیم Product نیاز خواهد داشت.

### Open Product Decisions not blocking Design Exploration

- قاعدهٔ tie-break میان چند دستهٔ شغلی مورد علاقه که همگی در بالاترین سطح تقدم قرار دارند، هنوز تعیین نشده است. این تصمیم مقدار جهت اصلی را تغییر می‌دهد، اما تک‌جهتی بودن خروجی و ساختار لازم برای Design Exploration را تغییر نمی‌دهد.
- Business Outcome و سنجهٔ موفقیت کسب‌وکاری این تغییر هنوز توسط Product تعیین نشده است؛ این خلأ صورت مسئله و رفتار لازم برای Design Exploration را تغییر نمی‌دهد.
- پشتیبانی پیاده‌سازی فعلی جست‌وجو از کف عددی دقیق حقوق تأیید نشد و UI عمومی فقط بازه‌های درشت و تک‌انتخابی دارد. این یک uncertainty پیاده‌سازی برای Technical Planning است و قاعدهٔ Product مصوبِ مقایسه با `MaxSalary` را تغییر نمی‌دهد.
